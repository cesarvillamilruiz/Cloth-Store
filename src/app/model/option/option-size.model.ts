interface IOptionSize {
    optionSizeId: string;
    name: string;
    componentName: string;
}

export class OptionSize implements IOptionSize{
    optionSizeId: string;
    name: string;
    componentName: string;
}